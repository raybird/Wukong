[package]
name = "wukong-tg-client"
edition.workspace = true
version.workspace = true

[lib]
name = "wukong_tg_client"
path = "src/lib.rs"

[dependencies]
reqwest = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[features]
# Expose MockTgClient to dependent crates' tests without compiling it into
# release builds. Enable via a dev-dependency: features = ["mock"].
mock = []
