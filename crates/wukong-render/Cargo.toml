[package]
name = "wukong-render"
edition.workspace = true
version.workspace = true

[lib]
name = "wukong_render"
path = "src/lib.rs"

[dependencies]
pulldown-cmark = { workspace = true, features = ["html"] }
